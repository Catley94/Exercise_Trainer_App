import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.catley.exercisetrainer',
  appName: 'exercise-trainer',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
